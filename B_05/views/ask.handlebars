<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <style>
        .main {
            text-align: center;
            margin-top: 30px;
        }

        .main span {
            font-size: 30px;
            font-family: sans-serif;
            padding-bottom: 30px;
        }
        form {
            margin: 30px;
        }
        form textarea {
            padding: 5px;
            font-family: sans-serif;
            font-size: 15px;
        }

        form button {
            display: block;
            margin: 30px auto;
            width: 120px;
            height: 40px;
            border-radius: 5px;
            color: #ffffff;
            font-weight: bold;
            background: #337AB5;
            font-size: 20px;
            font-weight: bold;
            font-family: sans-serif;
        }

        form button:hover {
            cursor: pointer;
        }
    </style>
    <title>Document</title>
</head>
<body>
    <div class="main">
        <span>Hãy hỏi cộng đồng một câu hỏi đúng/sai hoặc có/không</span>
        <form method="post" action="/ask"><!--method: get lấy dữ liệu, post: gửi data, action: đường link-->
            <textarea name="question" id="" cols="70" rows="10"></textarea>
            <button type="submit">Gửi</button>
        </form>
    </div>
</body>
</html>


